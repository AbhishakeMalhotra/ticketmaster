<%- include ../partials/header.ejs %>

    <!-- <%= concertList %> -->

  <!-- Main Content -->
  <main class="container">
    <div class="row">
        <h1><%= title %></h1>
        <br>
        <div>
            <a href="/concert/add" class="btn btn-primary align-self-end" role="button">
                <i class="fas fa-plus-circle"></i> 
                Add a new Item
            </a>                
        </div>

        <div class="table-responsive">
            <table class="table table-bordered table-striped table-hover">
                <!-- Header Row-->
                <tr>
                    <th class="text-center">Item</th>
                    <th class="text-center">Qty</th>
                    <th class="text-center">Status</th>
                    <th>Size</th>
                    <th class="text-center">Tags</th>
                    <th class="text-center" colspan="2">Actions</th>
                </tr>

                <!-- Repeatable Template Row -->

                <% for(let count = 0; count < ConcertList.length; count++) { %>
                    <tr>
                        <td class="text-center"><%= ConcertList[count].item %></td>
                        <td class="text-center"><%= ConcertList[count].qty %></td>
                        <td class="text-center"><%= ConcertList[count].status %></td>
                        <td>
                            Hight: <%= ConcertList[count].size.h %><br/>
                            Width: <%= ConcertList[count].size.w %><br/>
                            UOM: <%= ConcertList[count].size.uom %><br/>
                        </td>
                        <td class="text-center"><%= ConcertList[count].tags %></td>
                        <td class="text-center">
                            <a href="/concert/edit/<%= ConcertList[count]._id %>" class="btn btn-primary btn-sm">
                            <i class="fas fa-pencil-alt"></i></a>
                        </td>
                        <td class="text-center">
                            <a href="/concert/delete/<%= ConcertList[count]._id %>" class="btn btn-danger btn-sm">
                            <i class="fas fa-trash-alt"></i></a>
                        </td>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
  </main>
<%- include ../partials/footer.ejs %>